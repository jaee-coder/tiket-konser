@extends('layouts.app')
@section('title', 'Dashboard')
@section('content')
<div class="container mx-auto px-4">
    <div class="bg-gradient-to-r from-indigo-500 to-purple-500 rounded-2xl p-6 text-white mb-8 shadow-lg"><h2 class="text-2xl font-bold">Selamat datang, {{ Auth::user()->name }}!</h2><p class="opacity-90 mt-1">Kelola tiket dan lihat riwayat pemesanan Anda.</p></div>
    <div class="bg-white rounded-xl shadow-md p-6 mb-8"><h3 class="text-xl font-bold text-gray-800 flex items-center gap-2 mb-4"><i class="fas fa-ticket-alt text-indigo-600"></i> Tiket Saya</h3>@if($tikets->isEmpty())<div class="text-center py-8 text-gray-400"><i class="fas fa-ticket-alt text-5xl mb-2 text-gray-300"></i><p>Belum ada tiket. <a href="{{ route('home') }}" class="text-indigo-600 hover:underline">Pesan tiket</a> sekarang!</p></div>@else<div class="overflow-x-auto"><table class="min-w-full divide-y"><thead class="bg-gray-50"><tr><th class="px-4 py-3 text-left text-xs uppercase">Kode</th><th class="px-4 py-3 text-left">Konser</th><th class="px-4 py-3 text-left">Tanggal</th><th class="px-4 py-3 text-left">Kursi</th><th class="px-4 py-3 text-left">Status</th></tr></thead><tbody>@foreach($tikets as $tiket)<tr><td class="px-4 py-3 font-mono text-sm">{{ $tiket->kode_tiket }}</td><td>{{ $tiket->konser->nama_concert }}</td><td>{{ \Carbon\Carbon::parse($tiket->konser->tanggal)->format('d M Y') }}</td><td>{{ $tiket->nomor_kursi }}</td><td>@if($tiket->status=='paid')<span class="bg-green-100 text-green-800 px-2 py-1 rounded-full text-xs">Lunas</span>@else<span class="bg-yellow-100 text-yellow-800 px-2 py-1 rounded-full">Booking</span>@endif</td></tr>@endforeach</tbody></table></div>@endif</div>
    <div class="bg-white rounded-xl shadow-md p-6"><h3 class="text-xl font-bold text-gray-800 flex items-center gap-2 mb-4"><i class="fas fa-users text-indigo-600"></i> Referral</h3>@if($referrals->isEmpty())<p class="text-gray-500">Belum ada teman yang mendaftar menggunakan kode referral Anda.</p>@else<ul class="list-disc pl-5 space-y-1">@foreach($referrals as $ref)<li>{{ $ref->name }} ({{ $ref->email }})</li>@endforeach</ul>@endif<div class="mt-4 p-3 bg-gray-50 rounded-lg"><p class="text-sm text-gray-600">Kode referral Anda: <strong class="text-indigo-600 text-base">{{ Auth::user()->id }}</strong></p><p class="text-xs text-gray-500 mt-1">Bagikan kode ini ke teman, mereka bisa memasukkannya saat registrasi.</p></div></div>
</div>
@endsection